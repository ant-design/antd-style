/**
 * iframe: 600
 */
import { AppContainer } from 'antd-style';

import App from '../../common/demo';

export default () => {
  return (
    <AppContainer themeMode={'auto'}>
      <App />
    </AppContainer>
  );
};
